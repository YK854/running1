
<h4>投稿一覧</h4>
<p>各投稿の詳細ページに進みたい場合は対象のURLをクリックしてください</p>
<%= notice %>
<% if @books.empty? %>
  <h5>まだ投稿はありません！</h5>
  <p>
    投稿するには
    <%= link_to 'こちら', new_book_path %>
    をクリックするか<br>
    画面上部の新規投稿をクリックしてください！
  </p>
<% else %>
  <table>
    <tr>
      <th>投稿ID</th>
      <th>投稿タイトル</th>
      <th>投稿内容</th>
    </tr>
    <% @books.each do |book| %>
      <tr>
        <td><%= link_to book.id, book_path(book.id) %></td>
        <td><%= link_to book.title, book_path(book.id) %></td>
        <td><%= link_to book.body, book_path(book.id) %></td>
        <td><%= link_to "#{book.id}を削除", book_path(book.id), method: :delete %></td>
      </tr>
    <% end %>
  </table>


<% end %>